name: Upload Artifact

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Criar arquivo
        run: echo "Arquivo gerado no build" > resultado.txt

      - name: Upload do artefato
        uses: actions/upload-artifact@v4
        with:
          name: resultado-build
          path: resultado.txt